package com.example.kotlinknowledge.app.theme

class AppStyle {
    companion object{
        val appSize: AppSize = AppSize()
        val appRadius: AppRadius = AppRadius()
        val appPadding: AppPadding = AppPadding()
        val appFont: AppFont = PrimaryFont()
        val appColor: AppColors = AppColors()
    }
}